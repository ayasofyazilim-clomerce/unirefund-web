import SearchTransactionClient from "./client";

export default function SearchTransaction() {
  // Server-side operations could be performed here if needed
  // For example, checking if camera permissions are recommended for this user

  return <SearchTransactionClient />;
}
