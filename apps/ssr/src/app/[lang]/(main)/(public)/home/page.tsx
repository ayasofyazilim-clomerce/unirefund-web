import HomeClient from "./client";

export default function Home() {
  // Server-side operations could be performed here if needed
  // For example, fetching initial data, checking authentication, etc.

  return <HomeClient />;
}
