name: Lint
run-name: Linting the Pull request 🚀
on:
  pull_request_target:
    branches: [main]
    types:
      - opened
      - synchronize
      - edited
      - ready_for_review
permissions:
  pull-requests: write
  contents: write
jobs:
  lint_project:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    permissions:
      pull-requests: write
      contents: write
    steps:
      - run: echo "🎉 The job is linting the project"

      - uses: actions/checkout@v4
        with:
          submodules: true
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: |
            web:
              - 'apps/web/**'
            ssr:
              - 'apps/ssr/**'

      - name: Show which apps changed
        run: |
          echo "Web app changed: ${{ steps.changes.outputs.web }}"
          echo "SSR app changed: ${{ steps.changes.outputs.ssr }}"

      - uses: pnpm/action-setup@v2
        with:
          version: 9.7.1
        if: steps.changes.outputs.web == 'true' || steps.changes.outputs.ssr == 'true'

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"
        if: steps.changes.outputs.web == 'true' || steps.changes.outputs.ssr == 'true'

      - name: Install dependencies
        run: |
          npm install -g pnpm
          pnpm install
        if: steps.changes.outputs.web == 'true' || steps.changes.outputs.ssr == 'true'

      # Web app steps
      - name: Format web app
        run: |
          cd apps/web
          pnpm format
        if: steps.changes.outputs.web == 'true'

      - name: Lint web app
        run: |
          cd apps/web 
          pnpm lint:fix
        if: steps.changes.outputs.web == 'true'

      - name: Check types web app
        run: |
          cd apps/web
          pnpm type-check
        if: steps.changes.outputs.web == 'true'

      # SSR app steps
      - name: Format SSR app
        run: |
          cd apps/ssr
          pnpm format
        if: steps.changes.outputs.ssr == 'true'

      - name: Lint SSR app
        run: |
          cd apps/ssr 
          pnpm lint:fix
        if: steps.changes.outputs.ssr == 'true'

      - name: Check types SSR app
        run: |
          cd apps/ssr
          pnpm type-check
        if: steps.changes.outputs.ssr == 'true'

      - name: Show git diff
        run: git diff --name-only
        if: steps.changes.outputs.web == 'true' || steps.changes.outputs.ssr == 'true'

      - name: Commit changes
        uses: EndBug/add-and-commit@v9
        with:
          default_author: github_actions
          message: "chore: lint and format apps"
          push: true
        if: steps.changes.outputs.web == 'true' || steps.changes.outputs.ssr == 'true'
